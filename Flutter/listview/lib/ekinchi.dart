import 'package:flutter/material.dart';

class Ekinchi extends StatelessWidget {
  const Ekinchi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        leading: Column(
          children: [],
        ),
        actions: [Text(""), Text(""), Text("")],
        backgroundColor: Colors.blue,
      ),
      body: Text(""),
    );
  }
}
